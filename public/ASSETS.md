# /public assets to add

Four WebP images could not be committed through the GitHub API (it does not accept binary content). Add them here from the project zip (`exocore-systems-tanstack.zip`, delivered in chat):

- `hero.webp`
- `mudcloth.webp`
- `medallion.webp`
- `headshot.webp`

Easiest way: on GitHub, open this `public/` folder, click **Add file -> Upload files**, and drag the four `.webp` files in (GitHub handles binary uploads fine). Or unzip and `git push` from your machine.

The site references `/hero.webp`, `/mudcloth.webp`, `/medallion.webp`, and `/headshot.webp`.
